# Development Progress Tracker

## Current Phase: Phase 2 (Growth)

### Pending - Phase 2
- None

### In Progress
- None

### Completed recently
- [DONE] Follow-up on the /admin/ quick-setup panel — surfaced the real paid catalog
  (https://rankjee.com/courses/, e.g. "AI Automation Engineering Pro") in the "Create a course &
  assign faculty" form: added `catalog_course` (which paid course this LMS batch delivers) +
  `is_default_for_catalog` (auto-enroll buyers) fields, admin-only (mirrors the full
  `/admin/lms/courses/` page — `LmsCourseForm` already strips these fields for office/faculty).
  Also showed the linked catalog course title in the "Assign a student" course dropdown and the
  "Reassign faculty" list so it's clear which real product each LMS batch maps to. Verified
  live: dropdown lists all 27 active catalog courses straight from `core.models.Course` (same
  table that powers /courses/). Full `lms dashboard` suite still 45/45 green (purely additive —
  no new form fields on the model, just exposing existing `LmsCourseForm` fields in the compact
  panel). Rebuilt `app.css`.
- [DONE] "Make an easy students & courses assign method to faculty in /admin for superuser":
  1. **Closed a real gap** — a course's faculty owner could previously only be set at *creation*
     time (`LmsCourseForm`); there was no way to reassign an existing course. Added a new
     `reassign_owner` POST action to `lms/views.py::courses()` (admin/office only, validated
     against the same `lms_owner_queryset()` used by the create form — via a new shared
     `lms/services.py::lms_owner_queryset()` helper so create and reassign never drift). Wired
     an inline "Faculty: [dropdown] [Save]" control into `templates/lms/courses.jinja` per
     course (admin/office only; faculty still just see a static "Owner: X" label).
  2. **New "Faculty & course quick setup" panel directly on the superadmin Command Center**
     (`/admin/`, `admin_dashboard_v2.jinja`) — no more hunting for `/admin/lms/courses/` to do
     the 3 most common LMS setup actions:
     - Create a course + assign a faculty owner in one form
     - Assign a student to any existing course (course dropdown + student autocomplete)
     - Reassign the faculty on any of the most recent 8 courses inline
     All three POST straight to the existing, already-tested `lms:courses` view/actions (no new
     endpoint on the heavily-trafficked `dashboard:index` view — kept that view GET-only to
     minimize production risk) and land back on `/admin/lms/courses/` with a success message.
  Added `CourseOwnerReassignmentTests` (7 tests: admin/office can reassign, faculty forbidden
  even for their own course, unassign to platform-wide, dropdown visibility gating). Full `lms
  dashboard` suite: 45 tests green. Verified live: `/admin/` renders the new panel with all 3
  actions present for a superuser. Rebuilt `app.css` for the new panel's Tailwind classes.
- [DONE] Tightened LMS scoping further per explicit request: "admin creates courses -> faculty
  assigned to that course -> students assigned to that course -> in faculty/tutor dashboard and
  LMS show only that course's topic". Two parts:
  1. **"Your courses" panel on the Tutor/Faculty ops dashboard (`/admin/`)** — already showed
     multiple courses (`lms_courses_overview`, up to 6, via `LmsCourse.objects.filter(owner=user)`
     with a "Manage" link to the full list at `/admin/lms/courses/`) from the earlier dashboard
     upgrade. Verified this already satisfies "show multiple courses assigned to them" — no
     change needed there.
  2. **LMS home "Topics" sidebar was showing platform-wide (unbatched) topics unrelated to a
     faculty's own courses** — `lms/services.py::assignments_for_user` used to give faculty
     `Q(course__owner_id=user.id) | Q(course__isnull=True)` (their own courses PLUS every
     platform-wide/admin-broadcast assignment, treated as "shared curriculum"). Removed the
     `course__isnull=True` arm for faculty entirely — a faculty's LMS world (Topics sidebar,
     recent submissions/assignments, admin_stats counts, topic_detail, assignment_detail direct
     access) is now strictly the courses admin/office assigned them, nothing broader. Also
     updated `can_view_assignment` so a faculty can no longer open a platform-wide assignment's
     detail page directly (404→redirect) — matches `assignments_for_user`. Students are
     completely unaffected (still see platform-wide + their enrolled courses, as before).
     Mirrored the same restriction in the Django `/sd/` admin (`lms/admin.py::_owned_assignment_ids`
     and `LmsAssignmentAdmin.get_queryset`) for consistency — a non-superuser staff account
     browsing `/sd/` now also only sees their own courses, not platform-wide content.
     `faculty_student_scope`/`own_student_ids` (previous session's narrowing fix) are kept as
     defense-in-depth but are now mostly a no-op in practice since platform-wide never enters a
     faculty's assignment set at all anymore. Rewrote `FacultyOwnStudentScopeTests` to match
     (platform-wide assignment title/students now invisible to faculty entirely, not just
     narrowed; added `test_admin_sees_platform_wide_assignment_and_both_students` proving
     admin/office visibility is untouched). Verified live via a Client-based smoke script: a
     faculty's own-course topic shows on `/admin/lms/`, a platform-wide topic does not.
     Full `lms` suite: 38 tests green. Full `lms dashboard hometutor users tutor_study` suite:
     65 tests green. No migration, no CSS rebuild needed.
- [DONE] Reworked the LMS course setup pipeline per explicit request: "admin or office adds
  topic → faculty assigned to that course → student assigned to that course and faculty
  (faculty/tutor cannot see students' email)". Three changes, all in `lms/`:
  1. **Topics are admin/office-managed curriculum** — `lms/services.py::can_manage_topics(user)`
     (`is_lms_admin or is_lms_office`) now gates `topic_create`/`topic_edit` (was `is_lms_staff`,
     which wrongly let faculty mint topics; Office was wrongly blocked). `LmsAssignmentForm` now
     deletes `topic_mode`/`new_topic_title`/`new_topic_description` for non-admin users, so
     faculty creating an assignment only ever pick from the existing topic list — the "Create
     new topic" radio + inline fields only render for admin (`assignment_form.jinja` branches on
     `form.topic_mode` existing). `home.jinja`'s "+ New"/"Edit" topic links and
     `topic_detail.jinja`'s "Edit topic" link switched from `is_staff_lms` to
     `is_admin_lms or is_office_lms` (split from the "New assignment" link, which stays
     `is_staff_lms`-gated since that's still faculty's job).
  2. **Office can now create a course AND assign a faculty/tutor as its owner** — previously
     Office was fully blocked from `create_course`; only superuser-admin could set `owner`
     (faculty could only self-serve their own course, never assign someone else's). Renamed the
     `courses()` view's gate to `can_create_course = is_manager or is_office`; the owner-forced-
     to-self branch is now `if not (is_admin or is_office)`. `LmsCourseForm.__init__` splits
     `can_assign_owner` (admin OR office — gets the owner picker) from `is_admin` (admin only —
     gets the monetization-linked `catalog_course`/`is_default_for_catalog` fields, which stay
     off-limits to office). `courses.jinja`'s create-course form now shows for
     `can_create_course`; copy updated ("Office · course & student setup" /
     "Create a course and assign a faculty/tutor to teach it, then assign or remove students
     below.").
  3. **Faculty/tutor never sees another student's email** — the "Assign student" datalist on
     `/admin/lms/courses/` was showing `username · email` to EVERYONE with enrollment rights,
     including faculty (a real PII leak — faculty had no business need to see student emails,
     only admin/office need it to disambiguate similar usernames). `student_directory_options`
     gained `include_email: bool` (default True); `courses()` view now calls it with
     `include_email=(is_admin or is_office)`, stripping email for faculty. Backend email lookup
     fallback in `LmsCourseMemberForm.clean_username` is untouched — a faculty who already knows
     a student's email can still type it in and it resolves; they just never see it *suggested*.
  Added `lms/tests.py::TopicAndStudentPrivacyPermissionTests` (9 tests: faculty blocked from
  topic create/edit, office+admin allowed, faculty's assignment form has no create-new-topic
  option (admin's does), office can create a course + assign a faculty owner, faculty never
  sees a student's email in the datalist while admin/office do). Updated the two
  `OfficeOversightTests` cases that previously asserted office *couldn't* create courses (now
  the opposite is true by design) + added a catalog-fields-hidden-from-office check. Full `lms`
  suite: 35 tests green. Full `lms dashboard hometutor users tutor_study` suite: 62 tests green.
  No CSS rebuild needed — only reused existing Tailwind utility classes.
- [FIXED] Two more Tutor/Faculty gaps reported after the nav-unification pass:
  1. **Top nav missing LMS link for TUTOR/FACULTY** — `templates/base.jinja`'s desktop nav
     `current_role == 'TUTOR'` branch only had Demo Inbox/My Listing/Marketplace (marketplace-only);
     it never had an LMS link at all (unlike PARENT/VIP_USER/admin/default branches, which all did).
     Same gap in the mobile bottom-tab-bar TUTOR branch. Added an "LMS" link/tab to both (desktop:
     new first link; mobile: wrapped the 3 existing tabs + new LMS tab in the same
     `flex-1 justify-evenly` pattern PARENT already used, so 4 tabs distribute evenly). Verified via
     Client smoke test: both a TUTOR and a FACULTY account now render `<a href="/admin/lms/">LMS</a>`
     in the desktop nav.
  2. **Faculty saw OTHER faculty's students on their own `/admin/lms/` home page** — real data leak,
     not a permissions bug. `assignments_for_user` correctly includes platform-wide (unbatched,
     `course=None`) assignments for every faculty since that's shared curriculum content anyone can
     assign — but `home_sidebar_data` (top scores/best likes/latest comments) and `admin_home_stats`
     (pending count, students-submitted count, pending-topics list) and `home()`'s "recent
     submissions" feed all derived their per-student rows straight from that same assignment-id set,
     with no student-level scoping. Result: if student X (enrolled only with Faculty B) submitted to
     a platform-wide assignment, Faculty A saw X's name/submission/marks on their own dashboard too.
     Fix: added `lms/services.py::own_student_ids(user)` (every student enrolled in ANY course this
     faculty owns) and `faculty_student_scope(user)` (a `Q()` — `student_id__in=own_student_ids` OR
     `assignment__course__owner_id=user` — the second arm is belt-and-suspenders for own-course
     submissions even if enrollment was later removed; returns `None`/no-op for admin+office, who
     correctly keep full platform visibility). Applied it to `home_sidebar_data`'s `base` queryset,
     `admin_home_stats`'s `submissions_qs`, and `home()`'s `recent_qs`. Assignment/topic *visibility*
     counts (total_assignments, topics_covered) are untouched — shared curriculum listing is still
     fine for every faculty; only per-student submission rows are now scoped. Added
     `lms/tests.py::FacultyOwnStudentScopeTests` (5 tests) proving: scope is `None` for admin/office,
     `own_student_ids` is correctly per-faculty, the home page hides another faculty's student on a
     platform-wide submission, `admin_home_stats` pending/students-submitted counts are scoped, and a
     faculty's own course submission is never accidentally filtered out. Full suite (53 tests) green.
  No CSS rebuild needed — both fixes reused existing `nav-link`/mobile-tab utility classes, no new
  Tailwind classes introduced.
- [CHECKED + FIXED] Tutor/Faculty signup flow audit (user reported `https://rankjee.com/hometutor/my/profile/`
  showing the login page). Traced it: that page is `@login_required`, so an anonymous visit
  correctly bounces to `/users/login/?next=/hometutor/my/profile/` — `login.jinja` even shows the
  right copy for it ("Log in to continue your tutor search or demo requests", the `'hometutor' in
  next_url` branch) — not a bug, working as designed. Ran the full email-signup → role picker →
  tutor-onboarding-form → `TutorProfile` created → lands on `hometutor:my_profile` flow through the
  Django test client end to end — no errors, no stray messages, nav shows "Demo Inbox" right after.
  While auditing, found and fixed a real latent bug in the *general* onboarding gate
  (`users/views.py::onboarding_profile`, `core/middleware.py::OnboardingGateMiddleware`,
  `users/onboarding.py::incomplete_onboarding_url_name`): if any admin/office-provisioned account
  (FACULTY, CITY_ADMIN, GLOBAL_ADMIN, VIP_USER — anything outside
  `PUBLIC_SELF_ASSIGNABLE_ROLES = {STUDENT, PARENT, TUTOR}`) ever ended up with
  `onboarding_completed=False`, every page load would middleware-redirect them into
  `/users/onboarding/role/` <-> `/users/onboarding/profile/` forever — `onboarding_role` always
  forwards to `onboarding_profile` when no role-picker session flag is set, and the old
  `onboarding_profile` code bounced non-self-assignable roles straight back to `onboarding_role`
  with an error message, with nothing ever setting `onboarding_completed=True` to break out.
  `CustomUser.onboarding_completed` defaults to `True` so this couldn't fire for garden-variety
  admin-created accounts, but it's a live footgun (e.g. an admin unchecking "onboarding completed"
  on a Faculty account's `/sd/` record to "reset their intro" would lock them out of every page).
  Fix: `onboarding_profile` now marks onboarding complete and sends these roles straight to the
  dashboard instead of bouncing back to the picker, since there's nothing for them to self-configure
  in that wizard anyway (`RoleSelectionForm` only ever offers Student/Parent/Tutor). Verified with a
  FACULTY test account forced into the risky `onboarding_completed=False` state — before the fix
  this would loop; after the fix it resolves in 3 hops and lands on `/admin/` with `onboarding_
  completed` flipped back to `True`. Full suite still green (48 tests).
- [FIXED] "Tutor and Faculty conflict" — a FACULTY-role user clicking Inbox/Listing/Marketplace
  from their own `/admin/` dashboard (or the top-nav links, or `/study/`) was bounced back with a
  stray "Tutor access only. Please sign in with a tutor account." error banner, and the site nav
  showed them generic/admin links instead of the tutor marketplace links (Demo Inbox / My Listing
  / Marketplace). Root cause: the earlier role-unification work only updated `lms.services` and
  `dashboard.views._role_dashboard_context`/`_staff_only` — several *other* access checks across
  the app still did an exact `role == 'TUTOR'` match with no FACULTY branch:
  `hometutor.views._is_tutor_user` (gates the whole demo inbox / listing / marketplace app),
  `tutor_study.views.tutor_required` + 4 student/tutor redirect checks (the `/study/` notes &
  assignments upload area), 5 `/admin/study/*` dashboard-chrome wrappers in `dashboard/views.py`,
  and — the one with the widest blast radius — `templates/base.jinja`'s `current_role` variable,
  which drives the entire top nav, mobile bottom nav, and footer "Open tutor study" link for every
  page on the site. Fixed all of them so FACULTY is treated identically to TUTOR everywhere
  behavior is gated on role (`hometutor` app, `tutor_study` app, dashboard study wrappers, and
  `base.jinja`'s `current_role` normalization: `FACULTY` now maps to `'TUTOR'` for nav-branch
  purposes in one place instead of needing per-callsite awareness). Left untouched, intentionally:
  the public signup role picker (`users/onboarding.py`) and the VIP manual-account-creation tool
  (`dashboard/forms.py`) — FACULTY accounts are admin/office-provisioned, not self-service, so
  they correctly don't appear in those self-signup flows. Verified via Django test client with a
  real FACULTY account (`faculty_priya`): demo inbox / listing / marketplace / study routes all
  reachable with no stray error banner, nav shows "Demo Inbox" link; full `dashboard`/`hometutor`/
  `lms`/`users` suite still green (48 tests).
- [DONE] Full pass on the Faculty/Tutor dashboard (`/admin/` → `role_tutor.jinja` +
  `dashboard.views._role_dashboard_context`, TUTOR/FACULTY branch) — the previous version already
  had the LMS-teaching stat cards but still made a tutor hunt across 4+ separate panels (SLA
  banner, grading queue, stale-engagement note, preview-mode notice) to know what to do next, and
  had zero shared view of a tutor's students across the two disconnected systems (LMS course
  enrollments vs. marketplace engagements). Three additions, all reusing existing models/queries —
  no new migrations:
  1. "Today's priorities" — a single ranked, color-coded action list at the very top (replacing the
     scattered preview/SLA notices) built from data already computed for the KPI cards: missing
     listing (amber), demo SLA breach >24h (rose), submissions awaiting grading (rose, links to a
     new `#lms-grading` anchor on the same page), stale active engagements 7d+ (amber), and plain
     new demo requests (indigo) when nothing is overdue. Shows a green "You're all caught up"
     state when the list is empty instead of an empty section.
  2. "My students" roster — merges `LmsCourseEnrollment.objects.filter(course__owner=user)` and
     `TutorEngagement.objects.filter(tutor_profile=..., status=ACTIVE)` into one deduplicated table
     (student, LMS courses enrolled in, marketplace Active badge, one action link to the engagement
     Room or LMS courses page). Previously a tutor had no single place to see "who are my students
     and where do they show up."
  3. "Today's sessions" — a compact panel listing `SessionAttendance` rows `scheduled_for__date =
     today` across the tutor's engagements (this is the tutor-marks-attendance model, distinct from
     the student-self-reported `StudentDailyClassLog` used on the parent family hub), each linking
     to the engagement room to log/update it. Hidden entirely when there's nothing today.
  Also added a "+ New assignment" quick link to the top quick-links row (previously only available
  inside the LMS-teaching section). Verified via Django test client against the real `demo_tutor`
  account (200 OK, priorities + roster render); full `dashboard`/`hometutor`/`lms`/`users` suites
  green (48 tests); rebuilt `static/css/app.css` for the new table/list markup.
- [DONE] Promoted the `/admin_2/` dashboard rewrite to be the canonical `/admin/` Command
  Center. `dashboard.views.index`'s `_staff_only` branch now runs the same
  aggregation/render logic that used to live in the standalone `index_v2` view (real
  action-center queues, revenue/growth/balance-sheet, hurdle topics, inline task/demo/tutor-lead
  review) and renders `admin_dashboard_v2.jinja` instead of the old `admin_dashboard.jinja`.
  Removed the "(Preview)" title and the preview banner ("Dashboard rewrite preview... Back to
  classic /admin/") from that template since it's no longer a preview. `/admin_2/` (`index_v2`)
  is now just a one-line permanent redirect to `dashboard:index`, so old bookmarks/links keep
  working. The old `admin_dashboard.jinja` + all its stat-computation code is left completely
  untouched on disk (no longer referenced anywhere) as a rollback reference — to roll back,
  swap the `_staff_only` branch body back and restore the old `index_v2`. Access level is
  unchanged: still gated by the existing `_staff_only(request)` check (superuser OR is_staff
  excluding TUTOR/FACULTY) — not narrowed to superuser-only like the old `/admin_2/` preview was.
  One known nuance: the old dashboard scoped stats to a CITY_ADMIN-with-is_staff account's own
  state; the new one shows global numbers to everyone who passes `_staff_only` (no per-city
  scoping) — acceptable since real ops/admin `is_staff` accounts are expected to see
  platform-wide data. Verified: superuser sees the new dashboard at `/admin/`, `/admin_2/`
  redirects to `/admin/`, no "(Preview)" text anywhere; 48/48 tests still green.
- [DONE] Faculty/Tutor dashboard (`/admin/` → `role_tutor.jinja`) used to show ONLY hometutor
  marketplace metrics (demos, engagements, payouts) — zero visibility into actual LMS teaching
  work, even though that's now the primary self-serve flow. Added a new "LMS teaching" section
  (`dashboard.views._role_dashboard_context`, TUTOR/FACULTY branch) surfacing: course count,
  distinct student count, assignment count, and — the most actionable number — submissions
  awaiting grading (`LmsSubmission.status=SUBMITTED`, scoped to `course__owner=user`), each
  linking straight to `lms:assignment_detail` to review/grade. Also lists "Your courses" (with
  per-course student counts) and "Recent assignments", plus a "Create your first course" CTA
  when `lms_course_count == 0` (mirrors the existing hometutor-listing CTA pattern), and
  "Manage courses"/"+ New assignment" quick-action buttons. Ungraded submissions also feed into
  the existing `activity_timeline` alongside demo/engagement events. Rebuilt Tailwind CSS
  (`npm run build:css`) since the new section uses `bg-indigo-50/40`/`hover:bg-indigo-50` classes
  not previously compiled. Verified via manual render smoke test as `faculty_priya`, `demotutor`,
  `tutor_demo` (all render the new section); 48/48 tests still green.
- [DONE] Simplified "assign a student to a faculty/tutor" on `/admin/lms/courses/`. Two changes:
  (1) Office (CITY_ADMIN/GLOBAL_ADMIN) was previously 100% read-only in LMS — per explicit
  request they can now add/remove course members (enroll/unenroll students into ANY faculty's
  course) while course/assignment creation stays admin+faculty-only. New
  `can_manage_enrollment = is_lms_staff(user) or is_lms_office(user)` gate in `lms/views.py:courses`
  controls the "Assign student"/"Remove" UI and POST handling for `add_member`/`remove_member`
  only; `create_course` still explicitly checks `is_manager` and 403s for Office.
  (2) UX: "Add student" used to be a bare text box requiring the *exact* username. Now it's an
  autocomplete (`<datalist>`, native HTML, no JS) fed by `lms.services.student_directory_options()`
  (STUDENT-role users, capped at 300 — swap for an HTMX search endpoint like
  `hometutor.quick_tutor_search` if the platform outgrows that), and `LmsCourseMemberForm` also
  accepts an email as a fallback lookup, not just username. Bad input now shows
  "No student found matching ..." inline under the box instead of silently doing nothing.
  Tests: `lms.tests.OfficeOversightTests` updated/added (`test_office_can_add_and_remove_course_members`,
  `test_office_cannot_manage_enrollment_via_email_lookup_of_unrelated_user`,
  `test_office_can_view_courses_page_and_assign_students` replacing the old read-only assertion).
  48/48 tests green (dashboard, lms, users, hometutor, assessment, core) + manual smoke test of
  the assign→remove flow as `office_demo`.
- [DONE] SECURITY FIX: any TUTOR/FACULTY account with `is_staff=True` (the normal way faculty
  get scoped `/sd/` Django admin access, per `lms/admin.py`) was getting the FULL superadmin
  Command Center on `/admin/` — Content CMS (edit any question/video/skill), Backup Center
  (download/delete DB backups), every user's UPI withdrawal amounts, global tutor demo leads,
  and a global user search. Root cause: `dashboard.views._staff_only()` was just
  `is_staff or is_superuser`, with no role check, and `index()` had a second, separately
  duplicated copy of the same broken condition. Found live via a logged-in `demotutor`
  (role=FACULTY) account showing "FACULTY · Scope: Global" with full admin powers.
  Fix: `_staff_only()` now excludes TUTOR/FACULTY explicitly (`role not in (TUTOR, FACULTY)`)
  before falling through to `is_staff` — this single helper gates every CMS/backups/blog-gate
  call site in the file, so the fix applies everywhere at once, not just the dashboard landing
  page. Verified: `faculty_priya`/`faculty_arjun`/`demotutor` (all is_staff=True) now correctly
  get `_staff_only=False`; `admin` (superuser) and `office_demo` (GLOBAL_ADMIN, already scoped
  via its own role dashboard) unaffected. Direct URL attempts to `/admin/cms/` and
  `/admin/backups/` now redirect away instead of rendering.
- [DONE] Tutor and Faculty made the SAME role for LMS purposes (was two separate, confusing
  permission paths). `lms.services.is_lms_faculty()` now also grants access to any user with
  `role` TUTOR or FACULTY directly — no admin has to pre-create a course and hand them
  ownership first. Concretely: a plain marketplace Tutor (is_staff=False, is_lms_faculty=False)
  can now open `/admin/lms/courses/` and self-serve create their own first course (the "Add
  course" form was already faculty-safe — only asks for name/active, no owner/catalog fields);
  `is_lms_faculty` flag auto-flips True the moment they own any course either way. Also added
  FACULTY to `dashboard.views.index`'s role-dedicated-dashboard list and
  `_role_dashboard_context`, so a FACULTY-role user gets the exact same `role_tutor.jinja`
  workspace dashboard as a TUTOR (previously FACULTY fell through to the generic student
  dashboard, which made no sense for a teaching account) — plus a new "LMS courses" quick link
  on that dashboard pointing straight at `/admin/lms/courses/`. `LmsCourseForm`'s admin-only
  owner picker also now includes `role=FACULTY` (previously only TUTOR/is_staff/is_lms_faculty).
- [DONE] Whole-app breadcrumb navigation pass. Reused the existing `templates/partials/_breadcrumb.jinja`
  component (previously only wired up in `lms`/`tutor_study`) and rolled it out to the apps that actually
  needed it — prioritized by depth × zero-coverage, found via a full URL-hierarchy + breadcrumb-usage
  audit. Skipped genuinely flat apps (`users`, `blog`, `learning`, `server_buy` — 1-3 levels, already
  served fine by the top nav) to avoid breadcrumb noise on pages that don't need it.
  - `hometutor` (was the deepest gap: SEO landing pages go city → subject → class → location, up to 5
    levels, had ZERO breadcrumbs before). New `hometutor/services.py: crumb()`, `landing_crumbs()`,
    `tutor_detail_crumbs()` mirror the same escalation logic `tutor_city_landing` already uses to build
    `canonical_name`/`canonical_kwargs`, so the crumb trail and the canonical URL can never drift apart.
    Wired into `list.jinja` (marketplace + all SEO landing depths), `detail.jinja` (replaced its old
    hand-rolled 3-level "Home / Tutors / name" nav with a real city-aware trail), `compare.jinja`,
    `tutor_profile.jinja`, `tutor_demos.jinja`, `my_demo_requests.jinja`, `engagement_room.jinja`.
  - `dashboard` CMS section: one edit to the shared `cms_list_base.jinja` gives all 5 CMS list pages
    (paths/skills/questions/videos/tasks) a `Dashboard > CMS > <entity>` trail; `cms_form.jinja` /
    `cms_video_form.jinja` (the `/new/`, `/<pk>/` edit pages, previously only a bare "← CMS" link) got the
    same. Also upgraded `backups.jinja`, `students_score_table.jinja`, `attempt_review.jinja` (dynamic:
    includes "Students score table" only when staff reviewing someone else's attempt),
    `daily_class_log.jinja`, `admin_student_daily_logs.jinja`, `vip_blog_post.jinja` (blog create/edit),
    `vip_create_user.jinja` — all previously single "← Dashboard"-style links, now full trails.
  - `assessment`: `mock_test_landing` (exam → city SEO pages) and `certificate_view.jinja` — both had zero
    breadcrumbs before.
  - `core`: `course_cart.jinja` (`Courses > Cart`), replacing its old "← Back to courses" link.
  - Verified via the existing 46-test suite (all green) plus a manual smoke pass (as a superuser) over
    every touched URL confirming `aria-label="Breadcrumb"` renders with no template errors.
  - Deliberately NOT touched: `core/course_detail.jinja`'s existing custom hero-styled breadcrumb (works
    fine, different visual treatment for its hero section); `assessment/take_test.jinja` (mid-test page —
    adding a nav-away temptation during a timed exam is bad UX, not a navigation gap).
- [DONE] Superadmin dashboard rewrite — PREVIEW at `/admin_2/` (superuser-only), classic `/admin/`
  (dashboard.views.index / admin_dashboard.jinja) left completely untouched so nothing in prod can break.
  New `dashboard/services.py` (business logic layer, per CLAUDE.md) computes only real, directly-queried
  numbers — no fake trend arrows/percentages like the old page had:
  - `action_center()` — one row per queue a superadmin actually needs to act on: task submissions
    pending, UPI withdrawals pending (+ ₹ amount), tutor payout requests pending (+ ₹ amount, NEW —
    wasn't surfaced anywhere before), new tutor listings awaiting verification (NEW — TutorProfile.
    verification_status=PENDING wasn't surfaced anywhere before), demo requests pending, new tutor
    leads, course purchases missing an LMS batch (reuses lms.services.purchases_missing_lms_enrollment).
  - `revenue_summary()` — real SUCCESS-only money collected today/7d/30d, broken down by all 4 payment
    surfaces: core.CourseOrder, payments.PaymentOrder (Exam Pro), hometutor_payments.MarketplaceOrder,
    server_buy.StudentServerOrder.
  - `growth_summary()` — total users, new signups today/7d/30d, active-in-last-7-days (real DAU/WAU
    signal instead of the old page's decorative "engagement trend" avatar row).
  - `backup_health()` — flags if the latest BackupArtifact is >7 days old or missing.
  View: `dashboard.views.index_v2` (redirects non-superusers back to classic `/admin/`). URL wired at
  the top level in `rankjee/urls.py` (`admin_2/`, not nested under `dashboard.urls`, so it sits next to
  `/admin/` without touching that app's URLconf). Template: `templates/dashboard/admin_dashboard_v2.jinja`
  — single Action Center table + revenue/growth stat rows + the 3 highest-touch inline review tables
  (task submissions, demo requests, tutor leads) reusing the exact same POST endpoints as the classic
  page. Added a "🧪 New dashboard (preview)" link in the classic dashboard's sidebar (superuser-only) to
  reach it. Verified: `manage.py check` clean, full test suite green (46 tests), smoke-tested via test
  client as both a superuser (200, all sections render) and non-superuser staff (redirected to `/admin/`).
  Next step once approved: swap `/admin/` to render this view (or keep both — TBD after review).
  Follow-up after a deep model survey turned up trust & safety queues that were invisible on both
  the old and new dashboard: added "Open hometutor disputes" (EngagementDispute OPEN/IN_REVIEW),
  "Chat messages flagged for support" (EngagementChatMessage.support_flag), "Tutor KYC documents
  awaiting review" (TutorDocument.status=PENDING), and "Course referral commissions awaiting
  review" (CourseReferral.status=PENDING, + ₹ commission owed) to the Action Center — all link
  straight to the pre-filtered /sd/ admin list. Also split the hometutor revenue row to show the
  platform's actual net take (Sum(platform_fee_amount)) as a sub-row under the gross figure, since
  amount_gross includes the tutor's own share (matches the convention already used in the
  CITY_ADMIN/GLOBAL_ADMIN role dashboards).   Re-verified: full test suite green (46 tests), smoke
  test confirms all 4 new rows + the net-fee sub-row render.
  Second upgrade pass — added real signal that was still missing:
  - `week_over_week()` — genuine this-week-vs-prior-week % change for revenue and signups
    (returns None, never a fake number, when the prior window has zero history to compare against).
    Shown as ▲/▼ badges next to the "Last 7 days" revenue card and "Signups (7d)" growth card —
    this directly replaces the old dashboard's fabricated "↑ 12% vs last month".
  - `revenue_trend()` / `signup_trend()` — real 14-day daily bar charts (pure CSS height %, no JS
    charting lib), hover shows exact date + value. Percentages precomputed in the view (Decimal-safe)
    rather than done in Jinja.
  - `balance_sheet_summary()` — "Money the platform currently owes": unwithdrawn student wallet
    balances + pending UPI withdrawals + pending tutor payouts = Total owed, plus active Exam Pro
    subscription count. Founder-level snapshot, not a queue.
  - `hurdle_skills()` — restored the old dashboard's real "weakest tests by avg score" signal
    (content-quality, not decorative) since it was genuinely useful and got cut in the first pass.
  - Quick-actions bar (Add question/video/task, CMS, Backups, SEO, LMS, /sd/) + a "Jump to a user"
    search box (submits straight to `/sd/users/customuser/?q=`) — v1 had zero navigation, forcing a
    bounce back to classic `/admin/` for routine ops; this closes that gap without re-adding the old
    16-link sidebar.
  Fixed one bug during this pass: `UserSubscription` actually lives in `payments.models`, not
  `core.models` — caught immediately by the smoke test (ImportError), not by guessing. Re-verified:
  full test suite green (46 tests), `manage.py check` clean, service-level shell sanity checks on
  `_pct_change` edge cases (zero baseline → None, not a fake %), Tailwind rebuilt.
- [DONE] Parent family hub now surfaces attendance: `student_progress_summary` (users/services.py) pulls
  the student's last 14 `dashboard.StudentDailyClassLog` entries (self-reported Present/Absent, pre-existing
  model — was only visible to the student themselves and staff/admin before). `/users/family/` renders them
  as green/red chips under "Attendance (self-reported)" for any approved parent link. No migration needed
  (model already existed). Rebuilt Tailwind CSS (new chip classes). Verified via `manage.py test users lms`
  (46 tests, all pass) + seeded demo_student with 3 sample logs — see test_user.txt.
- [DONE] LMS multi-faculty isolation: renamed LmsBatch/LmsBatchMembership -> LmsCourse/LmsCourseEnrollment,
  added LmsCourse.owner (faculty). Permission gate = is_staff (faculty, own courses only) vs is_superuser
  (admin, sees everything). Added CustomUser.Role.FACULTY as a reporting label only (not the security gate;
  public signup unaffected). Scoped: assignments_for_user, can_view_assignment, can_manage_assignment (new),
  can_manage_course (new), admin_home_stats(user), home sidebar stats, /sd/ Django admin querysets.
  Faculty "Courses" page (was "Batches") now shows only their own courses; admin sees all + can assign owners.
  Assignment create/edit form requires faculty to pick one of their own courses (blocks accidental
  platform-wide leaks); admin may still leave it blank for platform-wide content.
  Migrations: users/0016_customuser_role_faculty.py, lms/0008_faculty_courses.py (applied locally, verified
  via makemigrations --check + manual isolation smoke test — see test_user.txt for faculty_priya/faculty_arjun
  demo accounts). NOT YET migrated on production (rankjee.com) — run `migrate` there, then assign real
  faculty as owners of existing courses via /sd/ or the Courses page.
- LMS home: 4-column layout — Topics | Recent submissions | Recent Assignment | Top score
- [DONE] fix-rankjee.md — wrote deep architecture review of the whole platform (lms vs tutor_study vs
  hometutor vs core.Course catalog vs assessment vs learning). Confirmed with user: Faculty == marketplace
  Tutor (one account/role), tutor_study stays permanent/separate (no merge planned), catalog→batch routing
  = single default/open batch. Implemented **Phase 3 (course→tutor assignment)** plus the minimal slice of
  **Phase 1** it depends on:
  - users.CustomUser.is_lms_faculty (new bool, decoupled from is_staff) — auto-set True the moment a user is
    assigned ownership of an LmsCourse. lms.services.is_lms_faculty() now checks this flag OR legacy
    is_staff-non-superuser, so a plain marketplace Tutor (role=TUTOR, is_staff=False) can become LMS faculty
    without touching /sd/ Django admin access.
  - lms.LmsCourse gained `catalog_course` (FK → core.Course, nullable) and `is_default_for_catalog` (bool).
    Owner field's limit_choices_to={'is_staff': True} removed (Tutors aren't Django staff).
  - lms.services: default_lms_course_for_catalog(), enroll_from_catalog_purchase(),
    purchases_missing_lms_enrollment() (admin-facing audit of paid-course purchases with no batch/tutor
    assigned yet).
  - core/services_course_checkout.py: complete_course_order_after_payment() now calls
    enroll_from_catalog_purchase() for each purchased course right after the CoursePurchase row commits —
    buyer is auto-enrolled into the catalog course's default LmsCourse batch (if a tutor has been assigned).
  - lms/forms.py LmsCourseForm: admin-only catalog_course + is_default_for_catalog fields; owner queryset
    now includes is_staff OR is_lms_faculty OR role=TUTOR accounts (not just is_staff).
  - templates/lms/courses.jinja: shows "Delivers: <catalog course title>" badge per batch, admin-only
    "N course purchases have no delivery batch yet" warning banner.
  - lms/admin.py: LmsCourseAdmin list_display/search/raw_id_fields updated for catalog_course.
  - Migrations: users/0017_customuser_is_lms_faculty.py, lms/0009_lmscourse_catalog_course_and_more.py
    (applied locally after confirmation, sqlmigrate dry-run reviewed first per workflow rules). NOT YET
    migrated on production — run `migrate` there before assigning any tutor to a paid course.
  - Verified end-to-end via Django shell: creating an LmsCourse with an owner auto-flips
    owner.is_lms_faculty True; enroll_from_catalog_purchase() creates exactly one LmsCourseEnrollment per
    buyer (idempotent on repeat calls); purchases_missing_lms_enrollment() correctly reports none once
    enrolled. Also verified /admin/lms/courses/ renders the new fields for admin and hides them for faculty.
    Full existing test suite (lms/users/core, 14 tests) still passes.
  - Remaining phases from fix-rankjee.md NOT yet built: Phase 2 (real ParentStudentLink model),
    Phase 4 (assessment.Skill link on LmsAssignment + course-scoped exam results rollup). Phase 5
    (retiring tutor_study) explicitly declined by user — tutor_study stays as the permanent 1:1 tool.
    [Both Phase 2 and Phase 4 completed later this session — see entries below.]
- [DONE] fix-rankjee.md Phase 2 — real Parent<->Student link (was previously just a role label with no
  data relationship). Built additively/isolated to minimize production risk (new table, new URL, new
  views/services/template — zero changes to any existing view's logic):
  - users.ParentStudentLink (new model): parent FK, student FK, relationship_label, is_verified,
    requested_at, verified_at. Unique constraint on (parent, student). Registered in /sd/ admin.
  - users/services.py (new file, business logic per CLAUDE.md rules): request_parent_link (Parent enters
    student username/email, creates a PENDING link + Notification to the student — validates roles,
    blocks self-linking, idempotent on repeat requests), approve_parent_link (student-only, flips
    is_verified + notifies parent), remove_parent_link (either side can reject/revoke, blocks strangers),
    linked_students_for_parent, links_for_student, can_parent_view_student, student_progress_summary
    (read-only snapshot: exam attempts, LMS enrollments, XP/streak/wallet — mirrors what the student
    already sees about themselves, nothing extra).
  - New page /users/family/ (users.views.family_hub, users/urls.py, templates/users/family.jinja):
    Parent side = request-link form + list of own links with read-only progress once approved.
    Student side = pending parent requests to approve/reject + ability to revoke later.
  - Small, additive-only link card added to templates/users/profile.jinja (visible only for
    role in PARENT/STUDENT) pointing to /users/family/ — no existing profile page content changed.
  - Migration: users/0018_parentstudentlink_and_more.py — brand-new table only, zero risk to existing
    data (verified via sqlmigrate dry-run before applying, applied locally after explicit confirmation).
  - Added users/tests.py::ParentStudentLinkTests — 13 new automated tests covering: pending-until-
    approved visibility gating, idempotent requests, role validation (non-parent/non-student rejected),
    unauthorized approve/remove attempts blocked, full view-level POST flows for both roles. Full suite
    now 27/27 passing (was 14/14 before this session's changes) — verified after every step.
  - NOT YET migrated on production — run `migrate` there before this feature is usable live.
- [DONE] fix-rankjee.md Phase 4 — link `/assessment/` graded tests into LMS assignments, with
  course-scoped results rollup. Purely additive, one new nullable FK, zero changes to existing
  assessment/exam logic:
  - lms.LmsAssignment.skill (new FK -> assessment.Skill, null/blank, on_delete=SET_NULL). Migration
    lms/0010_lmsassignment_skill.py — single ALTER TABLE ADD COLUMN, verified via sqlmigrate dry-run
    before applying (no data touched, no other table affected).
  - LmsAssignment.take_test_url() — deep-links to /assessment/<skill_id>/take/ for the linked test
    (mirrors the existing learning_lecture_url()/study_topic_url() pattern).
  - lms/services.py: student_skill_attempt_for_assignment(user, assignment) — that student's latest
    UserAttempt for the linked skill, or None. assignment_skill_roster(assignment) — for
    faculty/admin only: every enrolled student's latest attempt (or "not attempted"); returns []
    for platform-wide assignments (no course = no fixed roster) or assignments with no skill linked.
  - lms/forms.py LmsAssignmentForm: new optional "Graded test" field (Skill dropdown, active skills
    only, labelled with skill path for clarity).
  - lms/views.py assignment_detail: passes my_skill_attempt (student's own result) and skill_roster
    (faculty/admin only, gated by existing can_manage_assignment) into the template context — no
    change to any existing permission check, just additive context.
  - Templates: assignment_detail.jinja shows a "Take the test"/"Retake test" CTA + pass/fail badge
    for students, and a "Test results" roster panel for faculty/admin managing that assignment.
    assignment_form.jinja exposes the new field with help text.
  - lms/admin.py: LmsAssignmentAdmin list_display + raw_id_fields include the new skill field
    (existing get_queryset/save_model isolation logic untouched).
  - Added lms/tests.py (was an empty stub) — 10 new tests: auto is_lms_faculty flag on course
    ownership (regression guard), student attempt lookup (none/latest), roster correctness
    (enrolled-with-attempt, enrolled-without-attempt, empty for platform-wide, empty when no skill),
    and full view-level checks (student sees CTA + score, faculty sees roster with per-student rows,
    a different faculty is correctly denied — existing can_view_assignment 404→redirect behavior).
    Full suite now 37/37 passing (was 27/27 before this session's Phase 4 work).
  - `makemigrations --check` clean; migration applied locally after sqlmigrate dry-run review.
  - Local demo wired: "Priya assignment 1" now links the "Digital Marketing" skill test — see
    test_user.txt for the click-through demo flow.
  - NOT YET migrated on production — run `migrate` there before this feature is usable live.
  - fix-rankjee.md is now fully implemented (Phases 0–4); Phase 5 (retiring tutor_study) remains
    explicitly declined by the user.
- [DONE] fix-rankjee.md Phase 1 (remaining slice) — read-only Office (CITY_ADMIN/GLOBAL_ADMIN)
  oversight into LMS courses/assignments and exam-results rollup, per §4.2. Purely additive
  read paths — zero changes to admin/faculty management logic or permissions:
  - lms.services.is_lms_office(user) — True only for role in {CITY_ADMIN, GLOBAL_ADMIN}. Never
    combined into is_lms_staff (which stays the "can manage" gate for admin/faculty only) — kept
    as a fully separate check everywhere so Office can never accidentally get create/edit/review
    rights just by broadening an existing condition.
  - assignments_for_user / can_view_assignment / courses_for_user: Office now sees everything
    (like admin) for VIEWING only. can_manage_assignment / can_manage_course intentionally
    untouched (still only true for is_lms_admin/is_lms_faculty) — this is the enforcement point
    that keeps Office read-only everywhere (edit links, review forms, add/remove-member forms,
    create-course form all stay gated on the unchanged can_manage_*/is_lms_staff checks).
  - lms/views.py: home, topic_detail, courses, assignment_detail all updated to admit
    is_lms_office(user) into the *view* gate (previously only students/staff could even load
    these pages) while leaving every mutation path (POST handlers, assignment_create/edit)
    gated on is_lms_staff only — office POSTs to /admin/lms/courses/ now explicitly 403.
  - Templates: home.jinja/courses.jinja/assignment_detail.jinja show a "Office · read-only"
    label and hide all create/edit/add/remove controls when is_office_lms; the exam-results
    roster on assignment_detail is visible to Office too (marked "(read-only)") since exam
    results were explicitly called out in §4.2.
  - Added lms/tests.py::OfficeOversightTests — 9 new tests: role gating (is_lms_office true only
    for CITY_ADMIN/GLOBAL_ADMIN), the critical isolation guarantee that Office is NEVER counted
    as is_lms_staff/can_manage_*, cross-faculty course visibility, home/courses/assignment_detail
    render for Office with controls hidden, and 403 on any POST/create/edit attempt.
  - **Found and fixed an unrelated pre-existing bug** while adding these tests: `core/migrations/
    0010_course_page_content_json.py`'s SQLite idempotency loop reused one stale `apps.get_model()`
    reference across 4 `schema_editor.add_field()` calls; since JSONField requires a table
    rebuild on SQLite (not on Postgres), each rebuild used the pre-loop field list, silently
    dropping earlier-added columns (non-deterministic — sometimes raised a CHECK-constraint
    IntegrityError, sometimes just silently ended up missing course_includes/gain_outcomes/
    gain_perks/hero_usps). Never affected production (Postgres ADD COLUMN doesn't rebuild the
    table) — only bit a *fresh* SQLite database (new dev clone, or the test runner's in-memory
    DB), which is exactly what surfaced it once a test finally exercised `courses_for_user()`'s
    `select_related('catalog_course')`. Did NOT edit the existing 0010 migration (already applied
    everywhere) — added a new, purely-additive `core/migrations/
    0012_fix_course_json_fields_sqlite.py` that only runs on the sqlite vendor and only adds
    whichever of those 4 columns are still missing via plain `ALTER TABLE ADD COLUMN` (no CHECK
    constraint, so no table rebuild, so no data loss); a complete no-op on Postgres and on any
    SQLite db that already has the columns (verified against the real local db.sqlite3 — 0 rows
    changed). Verified the bug and the fix by migrating a brand-new empty SQLite file from zero.
  - Full suite now 46/46 passing (was 37/37 before this slice). `makemigrations --check` clean.
    Rebuilt `static/css/app.css` (`npm run build:css`) for the new template classes.
  - Local demo: `office_demo` (GLOBAL_ADMIN, password test1234) — see test_user.txt.
  - NOT YET migrated on production — run `migrate` there (the new 0012 migration is a safety
    no-op on Postgres, included only so SQLite environments — CI, fresh clones — stay correct).
- [FIXED] Styling broken on newer LMS/family pages — `static/css/app.css` is a checked-in, pre-built
  Tailwind file (`npm run build:css`), not compiled on the fly. It was last built 26 Jul, before this
  session added new templates/classes (courses.jinja, family.jinja, assignment_detail.jinja roster +
  CTA sections, assignment_form.jinja). Ran `npm install` (node_modules wasn't present) then
  `npm run build:css` to regenerate `app.css` with all current template classes — verified new file
  size increased and is served correctly by the dev server. Remember: any time new Tailwind utility
  classes are added to a `.jinja` template, re-run `npm run build:css` before it'll show up — the dev
  server does NOT compile Tailwind live.

### Completed - Phase 2 (Growth) Web Features ✅
- Leaderboard & XP points display
- Gamification badges (first test, first earn, referral, streaks)
- Wallet balance tracking independent of XP
- Referral system (code generation and tracking)
- Notification / daily reminder system
- Auto-approve task marketplace (domain matching)
- AI Personalization (difficulty, sequencing)
- Skill Paths System (Beginner → Pro → Earner)
- Background tasks (Celery + Redis)

### Completed - Phase 1 (MVP) ✅
- Analyzed durga.txt, created dev_plan.txt, test_user.txt
- Initialized Django 5 + Jinja2 + Tailwind + Alpine.js + HTMX project
- All modular apps: `core`, `users`, `dashboard`, `assessment`, `learning`
- CustomUser model (with streak_days, xp_points, last_active_date)
- User auth: Login, Signup (Jinja2-compatible form rendering)
- Daily login streak counter + profile page with test history
- Assessment engine: Skills, Questions (MCQ), UserAttempt model
- 10-question Digital Marketing test with results + weak area detection
- Retest lock logic (score >= 80% to pass)
- Micro-learning: Videos mapped to weak concept tags
- Earning tasks: Submission with proof, admin approval
- Custom Django Admin at /sd/ for all models
- Premium landing page redesign (gradient hero, how-it-works, stats bar, CTA)
- Seed data: 10 MCQ questions + 4 videos + 3 earning tasks
- dev_plan.txt updated with all Phase 1 complete
