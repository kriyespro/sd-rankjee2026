{% extends "dashboard/cms_list_base.jinja" %}
{% set title = "Earning tasks" %}
{% set subtitle = "Tasks are how users earn; keep them high-trust and verifiable." %}
{% set new_url = url('dashboard:cms_task_new') %}
{% block list_content %}
<form method="get" class="mb-4 flex gap-2">
  <input name="q" value="{{ q }}" placeholder="Search task title..." class="flex-grow rounded-xl border border-slate-200 px-4 py-2 text-sm">
  <button class="rounded-xl bg-slate-900 text-white px-4 py-2 text-xs font-black uppercase tracking-widest">Search</button>
</form>
<div class="bg-white border border-slate-200 rounded-2xl overflow-hidden">
  <table class="w-full text-left">
    <thead class="bg-slate-50 text-xs uppercase font-black tracking-widest text-slate-600">
      <tr>
        <th class="px-5 py-3">Title</th>
        <th class="px-5 py-3">Reward</th>
        <th class="px-5 py-3">Active</th>
        <th class="px-5 py-3 text-right">Edit</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-slate-100">
      {% for it in items %}
      <tr class="hover:bg-slate-50">
        <td class="px-5 py-3 text-sm font-bold text-slate-900">{{ it.title }}</td>
        <td class="px-5 py-3 text-sm font-black text-emerald-600">₹{{ it.reward_amount }}</td>
        <td class="px-5 py-3 text-sm">{{ "Yes" if it.is_active else "No" }}</td>
        <td class="px-5 py-3 text-right">
          <a class="text-indigo-600 font-bold text-sm hover:underline" href="{{ url('dashboard:cms_task_edit', it.id) }}">Edit</a>
        </td>
      </tr>
      {% else %}
      <tr><td colspan="4" class="px-5 py-10 text-center text-slate-500 font-bold">No tasks yet.</td></tr>
      {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %}

